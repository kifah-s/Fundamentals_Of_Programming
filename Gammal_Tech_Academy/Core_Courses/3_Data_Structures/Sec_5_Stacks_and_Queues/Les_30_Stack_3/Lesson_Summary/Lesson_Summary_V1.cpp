
//* >>>>> Lesson Summary (Version 1) .. <<<<< *//

#include <iostream>
#include <stack>
#include <ctime>

using namespace std;

//* Functions ..

// Print Lesson Summary / Function.
void lessonSummaryFun()
{
    cout << endl
         << "Lesson Summary (Version 1) .." << endl
         << endl;
}

//* End Functions ..

int main()
{
    lessonSummaryFun();

    /*
    //* Create stack and variable.
    stack<string> gammal;
    string answer;

    //* Push elements.
    gammal.push("Final exam");
    gammal.push("OOP");
    gammal.push("Algorithms");
    gammal.push("Data structures");
    gammal.push("C++");
    gammal.push("C");

    //* Verify whether the student can obtain the certificate or not.
    for (int i = gammal.size(); !gammal.empty(); i++)
    {
        cout << "Have you finished the " << gammal.top() << " course ? (yes or no)" << endl;
        cin >> answer;

        if (answer == "yes")
        {
            gammal.pop();
        }
        else
        {
            cout << "\nUnfortunately, you cannot obtain the certificate now.\n"
                 << endl;
        }
    }

    if (gammal.empty())
    {
        cout << "\nVery good, now you can get the certificate.\n"
             << endl;
    }
    */

    cout << endl;

    return 0;
}
